<% provide(:title, "編集") %>

<div class="container-fluid">
<%= render 'form' %>

<%= link_to 'Destroy', vacation_path(:callback_url => params[:callback_url]), 
                       method: :delete, data: { confirm: 'Are you sure?' },
                       :class => "btn btn-danger" %>

<%= link_to 'Back', params[:callback_url], :class => "btn btn-default" %>
</div>