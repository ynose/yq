<% provide(:title, "編集") %>

<%= render 'form' %>

<%= link_to t('destory'), vacation_path(:callback_url => params[:callback_url]), 
                       method: :delete, data: { confirm: t('destory_confirm') },
                       :class => "btn btn-danger" %>

<%= link_to t('back'), params[:callback_url], :class => "btn btn-default" %>
